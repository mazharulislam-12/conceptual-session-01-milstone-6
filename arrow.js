const add = (x, y) => {
    const result = x + y;
    return result;
}
console.log(add(5, 10));

const adds = (x, y) => x + y;
console.log(adds(2, 3));

const sum = (a, b) => a + b;
console.log(sum(10, 20));

const sub = (num1, num2) => num1 - num2;
console.log(sub(40, 30));

const show = () => {
    console.log('hello js');
}
show();

const showNumber = x => console.log(x);
showNumber(455);

const Number = x => Math.pow(x, 3);
console.log(Number(4));

